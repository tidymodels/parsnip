% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabular_chronos.R
\name{tabular_chronos}
\alias{tabular_chronos}
\title{Chronos-2 pretrained forecasting model}
\usage{
tabular_chronos(mode = "unknown", engine = "brulee")
}
\arguments{
\item{mode}{A single character string for the type of model. The possible
values for this model are \code{"quantile regression"} (the natural mode, which
returns a \code{hardhat::quantile_pred()}) and \code{"regression"} (which returns the
median point forecast). The mode must be set before fitting; for
\code{"quantile regression"} it is set with
\code{set_mode("quantile regression", quantile_levels = ...)}.}

\item{engine}{A single character string specifying what computational engine
to use for fitting. The only valid value is \code{"brulee"}.}
}
\description{
\code{tabular_chronos()} defines a pretrained time-series forecasting model that
produces quantile (distributional) forecasts. The network has fixed
pretrained weights, so no training is performed; the historical ("context")
data is ingested at fit time and the model forecasts a fixed horizon. This
function can fit quantile regression and regression models.

\Sexpr[stage=render,results=rd]{parsnip:::make_engine_list("tabular_chronos")}

More information on how \pkg{parsnip} is used for modeling is at
\url{https://www.tidymodels.org/}.
}
\details{
Unlike the other models in this package, Chronos-2 is pretrained and has no
tuning parameters. Forecast configuration is supplied through the engine with
\code{\link[=set_engine]{set_engine()}}, e.g. \code{set_engine("brulee", prediction_length = 14)}.
The available engine arguments mirror \code{\link[brulee:brulee_chronos]{brulee::brulee_chronos()}}:
\code{prediction_length}, \code{id_column}, \code{timestamp_column}, \code{model_id}, \code{revision},
\code{device}, and \code{cache_dir}. The \code{quantile_levels} are taken from the mode (via
\code{\link[=set_mode]{set_mode()}}) and forwarded to the fit automatically.

On first use the engine downloads the pretrained weights (about 500MB) and
caches them locally.

The \pkg{parsnip} interface forecasts a \strong{single series}. \code{predict()}
returns one row per horizon step (\code{.pred_quantile} or \code{.pred}), which cannot
unambiguously represent more than one series; supplying data with multiple
\code{id_column} values is therefore an error. For multi-series forecasting, call
\code{\link[brulee:brulee_chronos]{brulee::brulee_chronos()}} directly, where the id column is retained in the
output.
}
\examples{
\dontshow{if (!parsnip:::is_cran_check()) withAutoprint(\{ # examplesIf}
show_engines("tabular_chronos")

# Quantile (distributional) forecast
tabular_chronos() |>
  set_engine("brulee", prediction_length = 14) |>
  set_mode("quantile regression", quantile_levels = (1:9) / 10)

# Median point forecast
tabular_chronos() |>
  set_engine("brulee", prediction_length = 14) |>
  set_mode("regression")
\dontshow{\}) # examplesIf}
}
\references{
Ansari, A. F., Shchur, O., Küken, J., Auer, A., Han, B., Mercado, P., et al.
(2025). "Chronos-2: From univariate to universal forecasting."
\emph{arXiv preprint} arXiv:2510.15821.
}
\seealso{
\Sexpr[stage=render,results=rd]{parsnip:::make_seealso_list("tabular_chronos")} \code{\link[brulee:brulee_chronos]{brulee::brulee_chronos()}}
}
