# Prepend a new class

This adds an extra class to a base class of "model_spec".

## Usage

``` r
make_classes(prefix)
```

## Arguments

- prefix:

  A character string for a class.

## Value

A character vector.
