# Print helper for model objects

A common format function that prints information about the model object
(e.g. arguments, calls, packages, etc).

## Usage

``` r
model_printer(x, ...)
```

## Arguments

- x:

  A model object.

- ...:

  Not currently used.
