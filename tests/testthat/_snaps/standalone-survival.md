# convert survival data to factor

    'eval_time' should be a single, complete, finite numeric value.

---

    'eval_time' should be a single, complete, finite numeric value.

---

    'eval_time' should be a single, complete, finite numeric value.

---

    'eval_time' should be a single, complete, finite numeric value.

---

    'eval_time' should be a single, complete, finite numeric value.

