# tabular_chronos() accepts its modes

    Code
      tabular_chronos(mode = "classification")
    Condition
      Error in `tabular_chronos()`:
      ! "classification" is not a known mode for model `tabular_chronos()`.

